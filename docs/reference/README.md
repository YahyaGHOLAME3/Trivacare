# Références

Ce dossier conserve les anciens prototypes, variantes exploratoires et fichiers de travail déplacés hors de la racine active.

- `legacy-prototype/` contient l'ancienne version HTML/JSX à plat.
- `design-canvas.state.json` conserve l'état de l'outil d'exploration historique.
- `thumbnail` est conservé à titre d'archive.
